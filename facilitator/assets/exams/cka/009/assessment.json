{
  "questions": [
    {
      "id": "1",
      "namespace": "netpol-lab",
      "machineHostname": "ckad9999",
      "question": "Traffic policy in namespace `netpol-lab` is too permissive.\n\nThe namespace contains Pods named `frontend`, `api`, and `db`.\n\nCreate or fix NetworkPolicies so that:\n- `frontend` can reach `api` on TCP port `8080`\n- `api` can reach `db` on TCP port `5432` using normal cluster DNS/service discovery\n- direct traffic from `frontend` to `db` is denied\n- unrelated ingress to `api` and `db` is denied by default\n\nDo not modify the Pods or Services. Fix the policy layer only.",
      "concepts": ["networkpolicy", "networking", "security", "troubleshooting"],
      "verification": [
        {
          "id": "1",
          "description": "NetworkPolicies exist, select the intended Pods, and preserve DNS egress for api lookups",
          "verificationScriptFile": "q1_s1_validate_policy_presence.sh",
          "expectedOutput": "0",
          "weightage": 2
        },
        {
          "id": "2",
          "description": "Allowed paths frontend->api and api->db work on the required ports",
          "verificationScriptFile": "q1_s2_validate_allowed_paths.sh",
          "expectedOutput": "0",
          "weightage": 3
        },
        {
          "id": "3",
          "description": "Direct frontend->db traffic is denied",
          "verificationScriptFile": "q1_s3_validate_denied_path.sh",
          "expectedOutput": "0",
          "weightage": 2
        }
      ]
    }
  ]
}
