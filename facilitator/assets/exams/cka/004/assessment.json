{
  "questions": [
    {
      "id": "1",
      "namespace": "dns-lab",
      "machineHostname": "ckad9999",
      "question": "Cluster DNS resolution is failing.\n\nA deployment `web` and service `web` already exist in namespace `dns-lab`, and a test Pod `dns-check` is running with the default cluster DNS settings.\n\nDiagnose and fix the `CoreDNS` configuration in `kube-system` so that:\n- `dns-check` can resolve `web.dns-lab.svc.cluster.local`\n- `dns-check` can fetch HTTP content from the `web` service\n\nDo not recreate the application resources in `dns-lab`. Focus on restoring the cluster DNS service.",
      "concepts": ["troubleshooting", "coredns", "dns", "services", "networking"],
      "verification": [
        {
          "id": "1",
          "description": "CoreDNS in kube-system is restored to the cluster.local zone and available",
          "verificationScriptFile": "q1_s1_validate_coredns_config.sh",
          "expectedOutput": "0",
          "weightage": 3
        },
        {
          "id": "2",
          "description": "dns-check uses the cluster DNS path and resolves the service FQDN",
          "verificationScriptFile": "q1_s2_validate_dns_resolution.sh",
          "expectedOutput": "0",
          "weightage": 2
        },
        {
          "id": "3",
          "description": "dns-check can reach the web service over HTTP",
          "verificationScriptFile": "q1_s3_validate_http_reachability.sh",
          "expectedOutput": "0",
          "weightage": 2
        }
      ]
    }
  ]
}
