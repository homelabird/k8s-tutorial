{
  "questions": [
    {
      "id": "1",
      "namespace": "dnspolicy-lab",
      "machineHostname": "ckad9999",
      "question": "A pod DNS diagnostics brief is incomplete and contains stale or unsafe remediation guidance.\n\nA ConfigMap named `dns-diagnostics-brief` already exists in namespace `dnspolicy-lab`. Repair it so that:\n- `targetWorkload` is `dns-client`\n- `podInventory` is `kubectl get pod dns-client -n dnspolicy-lab -o wide`\n- `dnsPolicyCheck` is `kubectl get pod dns-client -n dnspolicy-lab -o jsonpath='{.spec.dnsPolicy}'`\n- `dnsNameserverCheck` is `kubectl get pod dns-client -n dnspolicy-lab -o jsonpath='{.spec.dnsConfig.nameservers[0]}'`\n- `dnsSearchCheck` is `kubectl get pod dns-client -n dnspolicy-lab -o jsonpath='{.spec.dnsConfig.searches[0]}'`\n- `dnsOptionCheck` is `kubectl get pod dns-client -n dnspolicy-lab -o jsonpath='{.spec.dnsConfig.options[0].name}'`\n- `resolverFileCheck` is `kubectl exec -n dnspolicy-lab dns-client -- cat /etc/resolv.conf`\n- `eventCheck` is `kubectl get events -n dnspolicy-lab --sort-by=.lastTimestamp`\n- `safeManifestNote` is `confirm dnsPolicy, dnsConfig nameservers, searches, and options before changing workload manifests or cluster DNS services`\n- save the repaired ConfigMap manifest to `/tmp/exam/q1/dns-diagnostics-brief.yaml`\n- save a plain-text checklist to `/tmp/exam/q1/dns-diagnostics-checklist.txt` with the sections `Pod DNS Inventory`, `dnsConfig Checks`, and `Safe Manifest Review`\n\nDo not delete the Pod, do not restart the Deployment, and do not patch cluster DNS services during this drill.",
      "concepts": [
        "dnsPolicy",
        "dnsConfig",
        "resolver",
        "troubleshooting"
      ],
      "verification": [
        {
          "id": "1",
          "description": "The DNS diagnostics brief ConfigMap uses the intended pod target, exact dnsPolicy and dnsConfig inspection commands, event evidence, and safe manifest guidance",
          "verificationScriptFile": "q1_s1_validate_dnspolicy_brief_contract.sh",
          "expectedOutput": "0",
          "weightage": 2
        },
        {
          "id": "2",
          "description": "The exported checklist contains the required sections and exact pod DNS and resolver inspection commands",
          "verificationScriptFile": "q1_s2_validate_dnspolicy_checklist_export.sh",
          "expectedOutput": "0",
          "weightage": 3
        },
        {
          "id": "3",
          "description": "The repaired ConfigMap manifest is exported and stale unsafe DNS remediation guidance is removed",
          "verificationScriptFile": "q1_s3_validate_dnspolicy_manifest_export_and_safety.sh",
          "expectedOutput": "0",
          "weightage": 2
        }
      ]
    }
  ]
}
